# Default ignored files
# The following files and directories will be ignored by default when searching or performing other operations.
# This can be useful for excluding temporary or unnecessary files and directories.
/shelf/
/workspace.xml

# Editor-based HTTP Client requests
# The following directory contains HTTP client requests that are generated by the code editor.
# These requests can be used for testing and debugging purposes, and can be modified as needed.
/httpRequests/

